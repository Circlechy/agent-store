# 贡献指南

感谢您对 Agent Store 的贡献感兴趣！本文档提供了贡献 agent、改进和文档的指南和说明。

## 如何贡献

我们欢迎各种形式的贡献：

- 🚀 **新 Agent**：提交您自己的 agent 实现
- 🐛 **错误修复**：修复现有 agent 中的问题
- 📝 **文档**：改进文档和示例
- 💡 **增强功能**：添加功能或改进现有 agent
- 🧪 **测试**：添加或改进测试覆盖率
- 🔍 **代码审查**：审查并为拉取请求提供反馈

## 开始使用

1. **Fork 仓库**：创建您自己的仓库分支
2. **克隆您的 Fork**：
   ```bash
   git clone https://github.com/your-username/agent-store.git
   cd agent-store
   ```
3. **创建分支**：
   ```bash
   git checkout -b feature/your-agent-name
   # 或
   git checkout -b fix/issue-description
   ```

## 贡献工作流

### 1. 对于新 Agent

#### 步骤 1：准备您的 Agent

- 确保您的 agent 遵循 [Agent 指南](AGENT_GUIDELINES.md)
- 包含所有必需文件（`metadata.json`、`README.md` 等）
- 删除任何敏感信息（API 密钥、令牌等）
- 彻底测试您的 agent

#### 步骤 2：放置您的 Agent

- 在 `community/` 中创建一个新文件夹，使用您的 agent 名称
- 使用小写和连字符（例如，`my-awesome-agent`）
- 确保文件夹名称与 `metadata.json` 中的 `id` 匹配

#### 步骤 3：提交您的贡献

1. **提交您的更改**：
   ```bash
   git add community/your-agent-name/
   git commit -m "Add your-agent-name agent"
   ```

2. **推送到您的 Fork**：
   ```bash
   git push origin feature/your-agent-name
   ```

3. **创建拉取请求**：
   - 转到 GitHub 上的原始仓库
   - 点击 "New Pull Request"
   - 选择您的 fork 和分支
   - 填写拉取请求模板
   - 提交 PR

### 2. 对于现有 Agent 的改进

1. **识别 Agent**：导航到 `community/` 中的 agent
2. **进行更改**：遵循相同的代码质量标准
3. **更新文档**：如果功能发生变化，更新 README
4. **测试您的更改**：确保现有功能仍然有效
5. **提交拉取请求**：遵循相同的 PR 流程

### 3. 对于文档改进

- 文档改进总是受欢迎的！
- 修复拼写错误，澄清说明，添加示例
- 直接通过拉取请求提交

## 拉取请求指南

### PR 标题格式

- 对于新 agent：`Add [agent-name] agent`
- 对于修复：`Fix [issue description] in [agent-name]`
- 对于增强：`Enhance [feature] in [agent-name]`
- 对于文档：`Update documentation: [description]`

### PR 描述模板

```markdown
## 描述
简要描述您的更改

## 更改类型
- [ ] 新 agent
- [ ] 错误修复
- [ ] 功能增强
- [ ] 文档更新
- [ ] 其他（请描述）

## Agent 名称（如适用）
[Agent 名称]

## 清单
- [ ] 代码遵循项目风格指南
- [ ] 已完成自我审查
- [ ] 为复杂代码添加了注释
- [ ] 已更新文档
- [ ] 没有新的警告
- [ ] 测试通过（如适用）
- [ ] 未包含敏感信息
- [ ] metadata.json 有效（对于新 agent）
```

### 审查流程

1. **自动检查**：您的 PR 将检查：
   - 有效的 `metadata.json`（对于新 agent）
   - 无敏感信息（API 密钥、令牌）
   - 代码质量和风格

2. **维护者审查**：维护者将审查您的 PR：
   - 代码质量和最佳实践
   - 文档完整性
   - 安全合规性
   - 整体贡献价值

3. **反馈**：您可能会收到要求更改的反馈
   - 及时处理所有反馈
   - 如有不清楚的地方请提问
   - 根据反馈更新您的 PR

4. **批准**：一旦获得批准，您的 PR 将被合并！

## 代码标准

### 一般原则

- **安全第一**：永远不要提交敏感信息
- **清晰的代码**：编写可读、可维护的代码
- **文档**：记录您的代码并提供示例
- **测试**：在适用的情况下包含测试
- **一致性**：遵循代码库中的现有模式

### 特定语言指南

#### Python
- 遵循 PEP 8 风格指南
- 在适当的地方使用类型提示
- 为所有公共函数/类包含文档字符串
- 使用有意义的变量名

#### JavaScript/TypeScript
- 遵循 ESLint 配置
- 使用 TypeScript 进行类型安全
- 包含 JSDoc 注释
- 遵循现代 ES6+ 模式

#### 其他语言
- 遵循特定语言的最佳实践
- 与现有代码保持一致

## 安全指南

### ⚠️ 关键规则

1. **永远不要提交敏感数据**：
   - API 密钥
   - 私有令牌
   - 密码
   - 密钥
   - 数据库凭据

2. **使用环境变量**进行配置：
   ```python
   # ✅ 正确
   api_key = os.getenv("API_KEY")
   
   # ❌ 错误
   api_key = "sk-1234567890"
   ```

3. **提供示例文件**（`.env.example`、`config.example.yaml`）

4. **提交前检查**：
   ```bash
   # 审查您的更改
   git diff
   
   # 检查常见密钥
   git diff | grep -i "api_key\|password\|secret\|token"
   ```

### 提交前清单

在提交 PR 之前，请验证：

- [ ] 没有硬编码的 API 密钥或令牌
- [ ] 代码或配置文件中没有密码
- [ ] `.env` 文件在 `.gitignore` 中
- [ ] 提供了示例配置文件
- [ ] README 记录了所需的 API 密钥

## 测试

### 对于新 Agent

- 测试核心功能
- 测试错误处理
- 使用示例输入进行测试
- 记录如何运行测试

### 对于改进

- 运行现有测试以确保没有破坏任何功能
- 为新功能添加测试
- 如果行为发生变化，更新测试

## 文档

### 必需文档

- **README.md**：您的 agent 的全面指南
- **代码注释**：解释复杂逻辑
- **示例**：提供使用示例

### 文档标准

- 使用清晰、简洁的语言
- 包含代码示例
- 解释配置要求
- 记录依赖项
- 提供故障排除提示

## 提交消息指南

编写清晰、描述性的提交消息：

```
添加 metadata.json 验证脚本

- 验证架构版本
- 检查必需字段
- 提供有用的错误消息
```

### 提交消息格式

- 使用祈使语气（"Add" 而不是 "Added"）
- 第一行应简洁（50 个字符或更少）
- 如需要，添加详细描述
- 引用问题："Fix #123"

## 问题或疑问？

- **一般问题**：打开 GitHub Discussion
- **错误报告**：打开包含详细信息的 Issue
- **功能请求**：打开包含用例描述的 Issue
- **安全问题**：直接通过邮件联系维护者（不要打开公开 issue）

## 行为准则

### 我们的标准

- 尊重和包容
- 欢迎新人并帮助他们学习
- 专注于建设性反馈
- 尊重不同的观点和经验

### 不可接受的行为

- 骚扰或歧视性语言
- 人身攻击
- 恶意挑衅或煽动性评论
- 发布他人的私人信息

## 认可

贡献者将：

- 列在项目的贡献者列表中
- 在 agent 元数据中署名
- 在发布说明中为重大贡献致谢

## 获取帮助

如果您需要帮助：

1. 查看现有文档
2. 查看类似的 agent 作为示例
3. 在 GitHub Discussions 中提问
4. 为错误或功能请求打开 issue

## 许可证

通过贡献，您同意您的贡献将在 Apache License 2.0 下许可，与项目相同的许可证。

---

感谢您为 Agent Store 做出贡献！您的努力使这个项目对每个人都更好。🎉
